import { introCollections } from './constants';
import styles from './ICNFTCollection.module.scss';

const ICNFTCollection = () => {
  return (<div className={styles.introCollectionContainer}>
    <div>
    </div>
  </div>)
};

export default ICNFTCollection;